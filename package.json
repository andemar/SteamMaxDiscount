{
  "name": "plugin-steam-max-discount",
  "version": "1.0.0",
  "private": true,
  "description": "Decorates Steam wishlist rows with max-discount status icons.",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/copy-build.mjs",
    "watch": "tsc -p tsconfig.json --watch",
    "test": "node --import tsx --test test/discountLogic.test.ts",
    "clean": "rimraf dist background content core"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "rimraf": "^5.0.5",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
